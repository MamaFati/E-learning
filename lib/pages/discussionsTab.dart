import 'package:flutter/material.dart';
class discussionsTab extends StatelessWidget {
  const discussionsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Descussions will be displayed here!!"),
    );
  }
}