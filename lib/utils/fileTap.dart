import 'package:flutter/material.dart';
class fileTap extends StatelessWidget {
  const fileTap({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Files will be displayed here")),
    );
  }
}